(()=>{const esc=s=>String(s).replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m])),clean=s=>{try{s=String(s)}catch{return""}return"none"===s?"":((s.startsWith('"')&&s.endsWith('"')||s.startsWith("'")&&s.endsWith("'"))&&(s=s.slice(1,-1)),s.trim())},out=[];document.querySelectorAll("*").forEach(el=>{let b="none",a="none";try{b=getComputedStyle(el,"::before").content}catch{}try{a=getComputedStyle(el,"::after").content}catch{}const parts=[],cb=clean(b),ca=clean(a);cb.length>2&&parts.push(["before",cb]),ca.length>2&&parts.push(["after",ca]);if(parts.length){let sel=(el.tagName||"").toLowerCase();el.id&&(sel+="#"+el.id),el.classList&&el.classList.length&&(sel+="."+Array.from(el.classList).slice(0,4).join(".")),out.push({sel:sel||"(node)",parts})}});const status=out.length?"warn":"ok",badge="ok"===status?"OK":"FOUND",items=out.length?out.map((x,i)=>`<div class="item"><div><b>${i+1}. ${esc(x.sel)}</b></div>${x.parts.map(([pos,val])=>`<div class="sub"><code>::${esc(pos)}</code> → <code>${esc(val)}</code></div>`).join("")}</div>`).join(""):`<div class="item"><div class="oktxt">Kein per CSS eingebundener Text (&gt;2 Zeichen) über <code>::before</code> oder <code>::after</code> gefunden.</div></div>`,html=`<!doctype html><html><head><meta charset="utf-8"><title>CSS Text Check</title><style>body{font:14px system-ui,sans-serif;margin:16px;color:#111}h1{font-size:18px;margin:0 0 6px}.url{font-size:12px;color:#555;margin-bottom:16px;word-break:break-all}.box{border:1px solid #ddd;border-radius:10px;padding:12px;margin-bottom:12px}.head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px}.title{font-size:15px;font-weight:700}.badge{display:inline-block;padding:3px 10px;border-radius:999px;font-size:12px;font-weight:700;border:1px solid}.badge.ok{background:#eaf7ed;color:#187a34;border-color:#b7dfc2}.badge.warn{background:#fff7e8;color:#9a6700;border-color:#f3d08b}.badge.fail{background:#fdecec;color:#b42318;border-color:#f1b2b2}.meta{color:#444;line-height:1.5}.item{padding:8px 0;border-top:1px solid #eee}.item:first-child{border-top:0}.sub{margin-top:6px;color:#555;font-size:12px}.oktxt{color:#187a34}.failtxt{color:#b42318}code{font-family:monospace;background:#f6f6f6;padding:1px 4px;border-radius:4px}</style></head><body><h1>Checks</h1><div class="url">${esc(location.href)}</div><div class="box"><div class="head"><div class="title">1. CSS text via pseudo-elements</div><span class="badge ${status}">${badge}</span></div><div class="meta">Geprüft wurden alle DOM-Elemente auf <b>::before</b> und <b>::after</b>.<br>Treffer (&gt;2 Zeichen): <b>${out.length}</b><br>Hinweis: Es wird nur <b>content</b> aus Pseudo-Elementen erkannt.</div><div style="margin-top:10px">${items}</div></div></body></html>`;const w=window.open();if(!w)return void alert("Popup blockiert.");w.document.open(),w.document.write(html),w.document.close()})();