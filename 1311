(()=>{const esc=s=>String(s).replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m])),selOf=el=>{let s=(el.tagName||"").toLowerCase();return el.classList&&el.classList.length&&(s+="."+Array.from(el.classList).slice(0,6).join(".")),el.id&&(s+="#"+el.id),s||"(node)"};const issues=[],htmlEl=document.documentElement,hasLang=htmlEl.hasAttribute("lang"),langValue=(htmlEl.getAttribute("lang")||"").trim(),hasXmlLang=htmlEl.hasAttribute("xml:lang"),xmlLangValue=(htmlEl.getAttribute("xml:lang")||"").trim();hasLang?langValue||issues.push({sel:selOf(htmlEl),errs:["Das <html>-Element hat ein leeres lang-Attribut"]}):issues.push({sel:selOf(htmlEl),errs:["Dem <html>-Element fehlt das Attribut lang"]});const totalChecks=1,failedChecks=issues.length?1:0,status=failedChecks?"fail":"ok",badge=failedChecks?"FEHLER":"OK",details=[];details.push(`<div><b>&lt;html&gt;</b> gefunden: <b>ja</b></div>`),details.push(`<div><b>lang vorhanden:</b> <b>${hasLang?"ja":"nein"}</b></div>`),details.push(`<div><b>lang-Wert:</b> <b>${langValue?esc(langValue):"—"}</b></div>`),details.push(`<div><b>xml:lang vorhanden:</b> <b>${hasXmlLang?"ja":"nein"}</b></div>`),hasXmlLang&&details.push(`<div><b>xml:lang-Wert:</b> <b>${xmlLangValue?esc(xmlLangValue):"—"}</b></div>`);const items=issues.length?issues.map((x,i)=>`<div class="item"><div><b>${i+1}. ${esc(x.sel)}</b></div>${x.errs.map(e=>`<div class="sub failtxt">${esc(e)}</div>`).join("")}</div>`).join(""):`<div class="item"><div class="oktxt">Die Seite hat am <code>&lt;html&gt;</code>-Element ein gesetztes und nicht-leeres <code>lang</code>-Attribut.</div></div>`,html=`<!doctype html><html><head><meta charset="utf-8"><title>Lang Check</title><style>body{font:14px system-ui,sans-serif;margin:16px;color:#111}h1{font-size:18px;margin:0 0 6px}.url{font-size:12px;color:#555;margin-bottom:16px;word-break:break-all}.box{border:1px solid #ddd;border-radius:10px;padding:12px;margin-bottom:12px}.head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px}.title{font-size:15px;font-weight:700}.badge{display:inline-block;padding:3px 10px;border-radius:999px;font-size:12px;font-weight:700;border:1px solid}.badge.ok{background:#eaf7ed;color:#187a34;border-color:#b7dfc2}.badge.warn{background:#fff7e8;color:#9a6700;border-color:#f3d08b}.badge.fail{background:#fdecec;color:#b42318;border-color:#f1b2b2}.meta{color:#444;line-height:1.5}.item{padding:8px 0;border-top:1px solid #eee}.item:first-child{border-top:0}.sub{margin-top:6px;color:#555;font-size:12px}.oktxt{color:#187a34}.failtxt{color:#b42318}code{font-family:monospace;background:#f6f6f6;padding:1px 4px;border-radius:4px}</style></head><body><h1>Checks</h1><div class="url">${esc(location.href)}</div><div class="box"><div class="head"><div class="title">1. lang-Attribut prüfen</div><span class="badge ${status}">${badge}</span></div><div class="meta">Geprüft wurde, ob das <b>&lt;html&gt;</b>-Element ein gesetztes und nicht-leeres <b>lang</b>-Attribut besitzt.<br>Prüfungen: <b>${totalChecks}</b><br>Fehlgeschlagen: <b>${failedChecks}</b><div style="margin-top:8px">${details.join("")}</div></div><div style="margin-top:10px">${items}</div></div></body></html>`;const w=window.open();if(!w)return void alert("Popup blockiert.");w.document.open(),w.document.write(html),w.document.close()})();